// project import
import navigation from './navigation';
import menu from './menu';
import users from './users';
import reports from './reports';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [navigation, menu, users, reports]
};

export default menuItems;
